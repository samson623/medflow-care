# App Mode: demo | prod
VITE_APP_MODE=prod

# Supabase
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_OAUTH_REDIRECT_URL=http://localhost:5173

# GPT-5 Nano (OpenAI API)
# OpenAI: set via Supabase secrets only â€” supabase secrets set OPENAI_API_KEY=sk-...
# Model ID (gpt-5-nano, gpt-5-mini, etc.)
VITE_OPENAI_MODEL=gpt-5-nano

# Push notifications: VAPID public key from supabase/functions/_shared/vapid.ts or web push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=